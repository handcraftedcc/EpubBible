{
  "name": "epub-bible",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "manifest:build": "python3 scripts/build_manifest.py",
    "serve": "python3 -m http.server 8000"
  }
}
